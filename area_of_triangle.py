#Program to calculate area of the triangle.

base = float(input("Enter Base in cm :"))
height = float(input("Enter Height in cm :"))

area = (1/2*base*height) #or, use 0.5*base*height
print("Area of the triangle is :", area)